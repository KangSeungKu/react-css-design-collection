import { style } from "@vanilla-extract/css";

export const fieldsetArea = style({
    marginBottom: 18,
})

export const legendArea = style({
    color: '#666696',
    fontWeight: '500',
    margin: '4px 0',
})

export const buttonsArea = style({
    display: 'flex',
    gap: 10,
})